# Build, test, sign and package Siren for Windows, macOS, and Linux
#
# Uses shared workflows from mythetech/workflows

name: Publish Desktop

on:
  workflow_dispatch:

# Required for reusable workflow to use Azure login (id-token) and upload artifacts (contents)
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: mythetech/workflows/.github/workflows/desktop-publish.yml@main
    with:
      app_name: "Siren"
      project_path: "Siren/Siren.csproj"
      test_project: "Siren.Test/Siren.Test.csproj"
      entitlements_path: "Siren/Siren.entitlements"
      icon_windows: "Siren/wwwroot/logo.ico"
      icon_linux: "Siren/wwwroot/logo5.png"
      icon_macos: "Siren/wwwroot/logo.icns"
      versioning: "run_number"
      storage_account: "stmythetechglobal"
      storage_container: "preview"
    secrets:
      # Windows Signing (org-level)
      AZURE_TRUSTED_SIGNING_ENDPOINT: ${{ secrets.AZURE_TRUSTED_SIGNING_ENDPOINT }}
      AZURE_TRUSTED_SIGNING_ACCOUNT_NAME: ${{ secrets.AZURE_TRUSTED_SIGNING_ACCOUNT_NAME }}
      AZURE_TRUSTED_SIGNING_CERTIFICATE_PROFILE_NAME: ${{ secrets.AZURE_TRUSTED_SIGNING_CERTIFICATE_PROFILE_NAME }}
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
      # macOS Signing (org-level)
      BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
      INSTALLER_CERTIFICATE_BASE64: ${{ secrets.INSTALLER_CERTIFICATE_BASE64 }}
      P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
      APPLE_USERNAME_ID: ${{ secrets.APPLE_USERNAME_ID }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      MACOS_SIGN_APP_IDENTITY: ${{ secrets.MACOS_SIGN_APP_IDENTITY }}
      MACOS_SIGN_INSTALL_IDENTITY: ${{ secrets.MACOS_SIGN_INSTALL_IDENTITY }}
      # App-specific secrets
      APPLE_APP_ID_PASSWORD: ${{ secrets.APPLE_SIREN_ID_PASSWORD }}
      BLOB_SAS_TOKEN: ${{ secrets.MYTHETECH_GLOBAL_BLOB_SAS_TOKEN }}
